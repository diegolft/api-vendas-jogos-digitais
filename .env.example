NODE_ENV=development
APP_PORT=3000
JWT_SECRET=troque-esta-chave-por-uma-segura
# Conexão: use DB_* (abaixo). Opcionalmente defina só DATABASE_URL em PaaS (Heroku, Railway, etc.).
# DATABASE_URL=postgresql://usuario:senha@host:5432/nome_do_banco
DB_HOST=localhost
DB_PORT=5432
DB_NAME=avjd
DB_USER=postgres
DB_PASSWORD=postgres
